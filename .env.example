# Copie este arquivo para ".env" apenas se for rodar localmente com "vercel dev".
# Em produção, configure estas variáveis em: Vercel > Settings > Environment Variables.

# Criada automaticamente quando você conecta o banco Neon ao projeto na Vercel
# (Storage > Connect Database). Não precisa preencher manualmente se usar a integração.
POSTGRES_URL=""

# Senha de acesso ao painel administrativo (admin.html). Escolha uma senha forte.
ADMIN_PASSWORD=""
